import { Link } from 'wouter'
import { useAuth } from '@/contexts/AuthContext'
export default function HomePage() { const { user, signOut } = useAuth(); return <main className="min-h-screen bg-gray-50"><header className="flex justify-between p-6 bg-secondary-500 text-white"><Link href="/" className="font-bold">Barbearia Web</Link><nav className="space-x-4">{user ? <><Link href={user.nivel === 'Administrador' ? '/painel' : '/minha-conta'}>Minha área</Link><button onClick={signOut}>Sair</button></> : <Link href="/login">Entrar</Link>}</nav></header><section className="max-w-3xl mx-auto p-16 text-center"><h1 className="text-4xl font-bold text-secondary-500">Seu horário, do seu jeito.</h1><p className="mt-4 text-gray-600">Agende serviços e acompanhe seus atendimentos online.</p><Link href="/agendamento" className="btn-primary inline-block mt-8">Agendar agora</Link></section></main> }
