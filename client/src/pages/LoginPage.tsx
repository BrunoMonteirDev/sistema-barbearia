import { useState, type FormEvent } from 'react'
import { useLocation } from 'wouter'
import toast from 'react-hot-toast'
import { useAuth } from '@/contexts/AuthContext'
export default function LoginPage() { const { signIn, signUp } = useAuth(); const [, go] = useLocation(); const [register, setRegister] = useState(false); const [nome, setNome] = useState(''); const [email, setEmail] = useState(''); const [senha, setSenha] = useState(''); const submit = async (e: FormEvent) => { e.preventDefault(); try { if (register) await signUp(nome, email, senha); else await signIn(email, senha); toast.success('Acesso realizado.'); go(register ? '/minha-conta' : '/'); } catch (error) { toast.error(error instanceof Error ? error.message : 'Erro ao autenticar.') } }; return <main className="min-h-screen grid place-items-center bg-gray-100 p-4"><form className="w-full max-w-sm space-y-4 bg-white p-6 rounded-xl shadow" onSubmit={submit}><h1 className="text-2xl font-bold">{register ? 'Criar conta' : 'Entrar'}</h1>{register && <input required className="input-field" placeholder="Nome completo" value={nome} onChange={e => setNome(e.target.value)} />}<input required type="email" className="input-field" placeholder="E-mail" value={email} onChange={e => setEmail(e.target.value)} /><input required minLength={6} type="password" className="input-field" placeholder="Senha" value={senha} onChange={e => setSenha(e.target.value)} /><button className="btn-primary w-full">{register ? 'Cadastrar' : 'Entrar'}</button><button type="button" className="w-full text-primary-600" onClick={() => setRegister(!register)}>{register ? 'Já tenho uma conta' : 'Criar conta de cliente'}</button></form></main> }
